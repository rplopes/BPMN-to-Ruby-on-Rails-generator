.block
  .secondary-navigation
    %ul.wat-cf
      %li.first= link_to "#{t("web-app-theme.list", :default => "List")}", offices_path
      %li.active= link_to "#{t("web-app-theme.new", :default => "New")}", new_office_path
  .content
    %h2.title
      = t("web-app-theme.new", :default => "New")
      Office
    .inner
      = form_for :office, :url => offices_path, :html => { :class => :form } do |f|
        = render :partial => "form", :locals => {:f => f}
- content_for :sidebar, render(:partial => 'sidebar')
